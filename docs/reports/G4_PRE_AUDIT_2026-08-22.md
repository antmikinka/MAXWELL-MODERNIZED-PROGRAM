---
type: audit-record
artifact: g4_pre_audit
gate: G4 (pre-audit)
date: 2026-08-22
auditor: independent fresh-context G4-PRE auditor (no participation in Waves 1-9 implementation, adjudication, or certification)
scope: Arts. 667-866 (Part IV, Ch XII-XXIII); G4 criteria C1-C4 + Wave-9 deliverables W9a/W9b
collab_reviewed: true
---

# G4-PRE Audit Report — Arts. 667-866

**Auditor:** independent G4-PRE auditor, fresh context. I built none of the audited work; every
verdict below was re-run or recomputed by me, not taken from prior reports. Prior gate reviews
(G2/G3), certifications (QUALITAS/SCRIBA), and orchestrator claims were treated as inputs only.
No source code or artifact was modified during this audit except the creation of this file.

---

## 1. Environment and commands run

- Repo: `C:\Users\antmi\Downloads\MAXWELL-MODERNIZED-PROGRAM`, Windows, Python 3.12.11 (miniforge3), pytest 8.4.2.
- Mandated invocation used for every pytest run: `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest tests -q` (bare `pytest` never used).

| # | Command | Result |
|---|---------|--------|
| 1 | `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest tests -q` (full suite) | **2523 passed, 0 failed, 0 errors, 0 xfailed, 3 warnings in 86.76 s, exit 0**; emitted `ARTICLE EVIDENCE MAP (667-866)` → `covered 200/200  missing 0  marked tests 501` |
| 2 | `python -m pytest tests/test_sympy_spine_wave8.py -q` (C1) | **172 passed** (168 parametrized verifier cases + 4 registry-guard tests); correctly wrote only the partial artifact |
| 3 | `python -m pytest tests/test_defects_s1.py -v` (C2) | **5 passed** (D-01..D-05 regression guards; zero xfail markers) |
| 4 | `python -m pytest tests/test_reference_store_wave9.py -q` (W9b) | **16 passed** |
| 5 | `python -c "... from articles import validate_store; print(validate_store())"` (W9a) | `[]` (clean) |
| 6 | `python scripts/anti_theater_lint.py` (task 7) | 365 files scanned, **1548 findings (13 HIGH)**, exit 1; by rule R1=1, R2=1, R6=11, R7=5, R8=1530 |
| 7 | `scripts/build_article_certifications.py` | **No dry mode exists** (no argparse/--dry/--check); running it would overwrite the SCRIBA record, so it was NOT executed. Instead its `load()` + `consistency_checks()` were invoked read-only via import: **0 problems** against the live artifacts |
| 8 | Programmatic reads (json/regex) of: `article_evidence_report.json`, `article_evidence_report_partial.json`, `article_evidence_report_G3_BASELINE.json`, `article_ledger.json`, `ARTICLE_CERTIFICATIONS_667_866.md`, `reference_values.json`, `verdicts.json`, both session manifests, `LAST200_STAGE3_QUALITY_REVIEW.md`, `S1_CERTIFICATION_2026-08-21.md`, `G2/G3_GATE_REVIEW_2026-08-21.md`, plus source spot reads (elliptic_integrals.py, magnetic_rotation.py, neumanns_theory.py, absolute_resistance.py, build_reference_store_wave9.py, sympy_verify registry) | as cited per criterion below |

Independent recomputations performed by the auditor: 8 reference-store values re-derived from
closed forms / scipy / standard constants; the Art-853 Neumann mutual inductance re-derived by a
brute-force 4000×4000-node double line integral; the 13 HIGH lint findings re-mapped to their
cited article numbers by direct decorator extraction; the manifest page→article union recomputed.

---

## 2. Per-criterion verdicts

### Task 1 — Full suite + evidence report: **PASS**

- Suite: **2523 passed / 0 failed / 0 errors / 0 xfailed** (3 warnings: pre-existing scipy `IntegrationWarning`s in `test_calculus_calculator.py`). Exit 0.
- Emitted line: `======================= ARTICLE EVIDENCE MAP (667-866) ========================` → `covered 200/200  missing 0  marked tests 501`.
- `docs/reports/article_evidence_report.json` after **my own run** (file mtime 2026-08-22 11:15:23 local = `generated: 2026-08-22T15:15:23Z`; `invocation.argv` matches my exact command; `keyword: null`, `markexpr: null`): `range [667, 866]`, `articles_covered: 200`, `articles_missing: []`, `gate_G3: "PASS"`, `partial: false`, `total_marked_tests: 501`. Recomputed the missing set independently from the evidence dict: empty; keys exactly 667-866.

### C1 — ≥60 SymPy spine verifiers: **PASS**

- Imported the registry: `ALL_SYMBOLIC_VERIFIERS` is a list of **69 unique callable verifier functions** (≥ 60 required; no duplicates by name).
- `tests/test_sympy_spine_wave8.py` run alone: **172 passed** — includes `TestG4VerifierCount` (registry ≥60, no duplicates, 56 Wave-8 verifiers all registered, all article targets in 667-866) and, for each of the 56 Wave-8 verifiers: `passed is True` with `relative_error < 1e-6`, full `VerificationResult` contract, and `@maxwell_cite`/article-marker consistency.

### C2 — Zero open S1/S2 defects: **PASS**

- `tests/test_defects_s1.py`: **5/5 passed** (D-01 Art 801/802, D-02 Art 702, D-03 Art 777, D-04 Art 812, D-05 Art 751). No `xfail` markers remain (repo suite summary reports zero xfailed/skipped).
- S2 pin tests all green within the full run: `test_defect_d12_weber_pin.py`, `test_d16_c_convention_consistency.py`, `test_defect_d22_dimensional_consistency.py`, plus the D-14 routing tests inside `test_articles_cylinders_harmonics_680_690.py` / math-spine files.
- Defect register (`docs/LAST200_STAGE3_QUALITY_REVIEW.md` §3) cross-walked against G2/G3 dispositions. For every S2 not among the five named closures I verified the current source state myself:
  - **D-13** (magnetic_rotation.py contradictory formulas): remediated in source — single consistent Art. 829 eq. (23)-(26) implementation, no `H_equiv` dead variable, Verdet-CS2 data with provenance. (See finding F3: closure is real but undocumented in gate records.)
  - **D-25** (Arts 765/766/856-858 meta-only): substantive implementations now present — `calc_absolute_resistance_capacitor_discharge` (765), `calc_recoil_damping_correction` (766), `neumann_reciprocity_residual` (856), `neumann_far_field_residual` (857), `motional_emf_coaxial` (858).
  - **D-28** (untested modules): superseded — the evidence map gives every in-scope article ≥1 marked test (200/200, min 1, max 15).
  - **D-39** (Art 702 hijack by `parameter()`): remediated — `elliptic_integrals.py::parameter()` deliberately cites no article, with an explicit D-39 remediation note.
- **Verdict: zero open S1/S2 in scope confirmed independently.**

### C3 — SCRIBA certification records: **PASS (with staleness finding F1)**

- `docs/reports/ARTICLE_CERTIFICATIONS_667_866.md` exists: **200 article rows, all tiered `**T3**`** (tier distribution counted programmatically: T3×200, nothing else).
- Generator `scripts/build_article_certifications.py` has no dry mode; it enforces internal consistency (200 contiguous articles, evidence header == recomputed totals, ledger 866 entries / no zero-citation articles, gate_G3 PASS) and exits loudly otherwise. I invoked `load()` + `consistency_checks()` read-only against the **live** artifacts: **0 problems** (live evidence preferred: 501 marked tests, PASS; ledger 866 entries / 3801 citations; ref store 200 in-scope articles).
- Spot-checks (3 articles) of doc row ↔ ledger ↔ evidence report ↔ store:
  - **Art 704**: ledger `cite_count 2` (elliptic_integrals.py `analyze_`/`verify_elliptic_integrals` — real numeric verification bodies, read in source), doc lists 2 impls / 1 test / ref no; live evidence now 7 tests; store now has `E_at_m_half_series`. Consistent with the frozen G3 baseline.
  - **Art 787**: ledger 18 citations (plane_wave.py, velocity.py, ...), doc 18 impls / 2 tests; live 8 tests; store `lambda_c_over_f_1e14`. Consistent.
  - **Art 853**: ledger 8 citations (neumanns_theory.py `calc_mutual_inductance_neumann`, `maxwell_mutual_inductance_closed_form`, ...), doc 8 impls / 1 test; qualifying test `test_art853_neumann_quadrature_matches_elliptic_closed_form` verified present. Consistent.
- Staleness: the doc's headline counts (333 marked tests, suite 2312, ref-store 71/200) are the **frozen G3 baseline** (`article_evidence_report_G3_BASELINE.json`, generated 2026-08-22T05:35:13Z, 333 tests — confirmed on disk). Current machine truth is 501 / 2523 / 200. The ledger (generated 00:58:14) similarly predates Wave 8 (only 13 `sympy_verify.py` citations recorded vs the 56 Wave-8 verifiers now live). All staleness **under-reports**; see F1.

### C4 — 100% page verdicts: **HUMAN-BLOCKED, out of verdict scope — HONESTY CHECK PASS**

- `page_verifier/data/verdicts.json`: `schema_version 1`, `count 7` == 7 entries, `updated_at 2026-08-18T04:18:20Z`. All 7 entries dumped and checked: `v1-p001..v1-p005`, `v1-p036` (arts [1,2]), `v1-p409` (art [254]) — **every one has `volume: 1`**; **zero `v2-*` page ids; zero Vol II pages 326-520; zero verdicts touching articles 667-866. No fabrication detected.**
- `docs/reports/PAGE_VERDICT_SESSION_MANIFEST_667_866.md` and `.json` both exist. The JSON `integrity` block states `volume_ii_verdicts: 0`, `in_scope_verdicts: 0`, "No verdict was fabricated in producing this manifest" — matching the live store exactly. Manifest claims independently recomputed: 195 session pages (`v2-p326` … `v2-p520` contiguous), union of carried articles **exactly {667..866}** (recomputed from the JSON `pages` array), `images_missing: 0`, 144 M / 6 R / 44 C / 1 B.
- The manifest does not pre-judge verdicts (all checkboxes empty; human-only write policy stated).

### W9a — reference store coverage and quality: **PASS**

- `validate_store()` → `[]`.
- Coverage recomputed from `tests/articles/reference_values.json`: **all 200 of 667-866 present, none missing**; 2 legacy out-of-scope entries (624, 625) also present (pre-existing, documented at G3; see F4). Total 256 values.
- 8 sampled entries across the range, each re-derived independently by me:

| Art | Key | Stored | Independent recomputation | Verdict |
|---|---|---|---|---|
| 670 | tangential_H_jump_unit_sheet | 4.191690043903363e-10 | 4π/c | AGREE (rel 1e-12) |
| 695 | solid_angle_on_axis_3_4_5 | 1.2566370614359172 | 2π(1−4/5) = 2π/5 | AGREE |
| 704 | E_at_m_half_series | 1.350643881047675 | scipy `ellipe(0.5)` | AGREE |
| 723 | force_balance_45deg_unit_mass | 980.665 | g_std·tan45° | AGREE |
| 765 | discharge_one_time_constant_fraction | 0.36787944117144233 | e⁻¹ | AGREE |
| 787 | lambda_c_over_f_1e14 | 2.99792458e-4 | c/1e14 | AGREE |
| 808 | round_trip_doubling | 2.0 (abs 1e-14) | Faraday non-reciprocity factor 2 | AGREE |
| 853 | mutual_inductance_far_limit | 0.019739208802178717 | 2π²a²b²/z³ at a=b=1,z=10 | AGREE (but see F2 on the tolerance rationale) |

  Every sampled entry has: numeric value, units string, exactly one of rel/abs tolerance, and a Class 2-5 provenance claiming independent derivation; none cites maxwell package output.
- **Independence verified at the source level:** `scripts/build_reference_store_wave9.py` imports only `json`, `math`, `fractions`, `pathlib` (+ `__future__`) — **zero maxwell imports** — with an explicit binding independence note; its own AGM and hypergeometric-series implementations are the derivation record. 129 builder-tagged values across exactly 129 articles (71→200 expansion arithmetic checks out); all 129 carry Class markers (Class 3×95, Class 2×10, Class 4×18, Class 5×6). Whole-store scan: **0 self-referential provenance strings, 0 malformed tolerance shapes, 0 empty provenance**.

### W9b — `tests/test_reference_store_wave9.py`: **PASS — substantive, not self-referential**

- Run alone: **16 passed**.
- Content assessment: (i) integrity belt-and-braces on top of `validate_store` (coverage 200/200, finiteness, tolerance shape, the 71→200 regression pin); (ii) provenance discipline (Class 2-5 markers required; a forbidden-phrase scan against "computed by maxwell"/"module output"/etc.); (iii) internal cross-consistency between independently-derived pins (694 kernel = 1.5·K − 2·E from the 703/704 pins; 703 K-pin vs 705 AGM-pin through K = π/(2·AGM)); (iv) external cross-checks against a third party (scipy `ellipk`/`ellipe`) and pin-vs-module probes (ESU/EMU charge ratio = c; PlaneWave λ = c/f; Gaussian E/B = c). Because the builder has no access to maxwell code, pin↔module agreement is genuine cross-validation, not self-confirmation. The battery carries no `@pytest.mark.article` marks (0 evidence-map contribution), which is correct — it guards the store, it is not article evidence.

### Task 7 — Anti-theater lint: **PASS (in scope)**

- `python scripts/anti_theater_lint.py`: 365 files, 1548 findings, **13 HIGH** (R1=1, R2=1, R6=11, R7=5, R8=1530).
- I independently extracted the cited articles of all 13 HIGH-decorated functions from source: 393/394 (conventions.py:300), 45 (charge.py:104), 552 (remaining_gaps.py:945 `verified = True`), 50-52 and 53-55 and 65×2 (force_theory.py), 12-19 (phenomena.py:1122), 404×2 (solenoidal.py), 444 (hysteresis.py:421), 76 (gauss.py:702), 447 (magnetostriction.py:557).
- **0 HIGH findings for articles ≥ 667; 13 HIGH for articles < 667** — the documented out-of-scope backlog, matching expectation exactly.

### Task 8 — Anti-fabrication of evidence artifacts: **PASS**

- Canonical `article_evidence_report.json` carries `partial: false` and full invocation provenance; the invocation recorded is a genuine bare full-suite run (`argv [..., 'tests', '-q']`, no keyword/markexpr/ignore) — and for this audit it is literally my own run's output (mtime cross-check), so it cannot be stale.
- `article_evidence_report_partial.json` is clearly quarantined: `partial: true`, `gate_G3: "NOT_EVALUATED_PARTIAL_RUN"`, distinct filename, full invocation provenance (currently shows my own mid-audit single-file runs). The conftest mechanism (`_is_partial_collection`) detects `-k`, `-m`, `--ignore`, `--deselect`, cache selection, node-ids, and non-root paths; partial runs never touch the canonical file. Observed working live during this audit (3 partial runs, canonical untouched).
- The frozen G3 baseline snapshot exists and is what the certifier falls back on; timestamps are mutually consistent.

---

## 3. Findings

| ID | Sev | Description | Evidence |
|----|-----|-------------|----------|
| **F1** | **S3 (minor)** | **Certification record and ledger are frozen at the G3 (Wave-7) baseline and under-report current Wave-8/9 truth.** `ARTICLE_CERTIFICATIONS_667_866.md` states 333 marked tests / suite 2312 / ref-store 71 articles; live machine truth is 501 / 2523 / 200 (all independently re-run here). `article_ledger.json` (generated 00:58:14) contains only 13 `sympy_verify.py` citation records although Wave 8 added 56 verifiers. Direction is strictly conservative (under-claiming), and the certifier's read-only consistency check against live artifacts returns 0 problems, so a regeneration would succeed. **Recommendation:** re-run `build_article_ledger.py` then `build_article_certifications.py` immediately before the formal G4 declaration so the certified numbers match the audited state. | §2 C3; commands 1, 7 |
| **F2** | **S3 (minor)** | **Reference-store entry 853 documents an incorrect tolerance rationale.** Provenance claims "Tolerance 5e-3 accommodates the O((a/z)²) finite-distance correction when consumed against the module at z = 10". Auditor's brute-force Neumann double line integral (4000×4000 nodes, no elliptics): M_exact = 0.0191650 vs leading term 0.0197392 → deviation **3.0%**, 6× the stated tolerance. No test currently consumes this pin against a module (Art 853's qualifying test does its own quadrature-vs-closed-form comparison), so present impact is nil — but a future consumer trusting the note would trip. The pin value itself is a correct z→∞ asymptote. | §2 W9a; brute-force computation above |
| **F3** | **S4 (cosmetic)** | **D-13's remediation is not documented in any gate record.** G2 check 6 lists neither D-13 among verified-remediated nor among open items, and G3 §5 covers only the five named S2s. Source inspection confirms the fix is real (single Art. 829-consistent formula; dead variable removed). Documentation-traceability gap only. | §2 C2; `maxwell/vortex_engine/magnetic_rotation.py` |
| **F4** | **S4 (cosmetic)** | Reference store retains two legacy out-of-scope entries (Arts 624, 625, consumed by `test_lint_remediation_last200.py`). Pre-existing and documented at G3 ("73 article keys incl. Arts 624/625 out of scope"); harmless to in-scope claims, noted for completeness. | §2 W9a |

No S1 (blocking) or S2 (major) findings. No theater, fabrication, or self-confirmation detected anywhere sampled.

---

## 4. OVERALL VERDICT

## **G4-PRE PASS-WITH-FINDINGS**

Every machine-verifiable G4 criterion was independently reproduced from scratch: the full suite
(2523/0/0/0) with a 200/200, 501-test evidence map written by my own run (C1's 69-registry green
standalone; C2's five S1 guards and all S2 pin tests green with zero open in-scope S1/S2 confirmed
by direct source inspection of every residual register entry; C3's 200×T3 record consistent with
its machine artifacts under the generator's own consistency logic; C4's honesty check clean with
zero Vol II verdicts and an honestly-empty 195-page session manifest); the Wave-9 store validates
empty, covers all 200 articles with genuinely independent builder derivations (stdlib-only builder,
8/8 sampled values recomputed to agreement, zero self-referential provenance), and its 16-test
battery is substantive; anti-theater lint shows 0 in-scope HIGH (13 mapped to Arts < 667). The two
S3 findings are staleness/under-reporting (F1) and an inaccurate tolerance rationale in one
currently-unconsumed store entry (F2); the two S4 findings are traceability cosmetics. None block.
C4's actual page verdicts remain the human-blocked input and are, per instruction, outside this
verdict. **Recommended before formal G4:** regenerate ledger + certification record (F1), correct
the 853 provenance note (F2), then convene the human page-verdict session.

---
*Auditor: independent fresh-context G4-PRE auditor, 2026-08-22. No implementation or fixes were
performed during this audit; no artifact was relied upon without independent reproduction. This
file is the sole deliverable written.*
